import { CodeBlock, Dependencies } from '@/components/docs/component-installation'
import { CLICommand } from '@/components/docs/cli-command'
import { PropsTable } from '@/components/docs/props-table'
import { ComponentPreview } from '@/components/docs/component-preview'

# Rolling Ball Scroll Indicator

A playful scroll indicator with a rolling ball that moves and rotates based on scroll progress.

<ComponentPreview
  component={
    <div className="relative w-full rounded-lg overflow-hidden bg-black">
      <video autoPlay loop muted playsInline className="w-full h-auto max-h-[600px] object-cover">
        <source src="https://pub-affb2190b74549c4a9fde667b3cb7485.r2.dev/demos/rolling-ball-scroll-indicator.mp4" type="video/mp4" />
      </video>
    </div>
  }
  code={`import { RollingBallScrollIndicator } from '@/components/ui/rolling-ball-scroll-indicator'

export function Demo() {
  return (
    <>
      <RollingBallScrollIndicator scrollContainerId="scroll-area" />
      <div id="scroll-area" className="h-[400px] overflow-y-auto">
        {/* Scrollable content */}
      </div>
    </>
  )
}`}
/>

## Install using CLI

<CLICommand componentName="rolling-ball-scroll-indicator" />

## Install Manually

<Dependencies step={1} title="Install dependencies">
    <CodeBlock code="npm install framer-motion" />
</Dependencies>

<Dependencies step={2} title="Copy the source code">
    <p className="text-[13px] text-muted-foreground mt-2 mb-8">Copy the code into <code className="text-foreground font-mono text-xs bg-neutral-100 dark:bg-[#161616] border border-neutral-200 dark:border-neutral-800 px-2 py-1 rounded-md mx-1">components/ui/rolling-ball-scroll-indicator.tsx</code></p>
</Dependencies>

## Usage

<CodeBlock language="tsx" code={`import { RollingBallScrollIndicator } from "@/components/ui/rolling-ball-scroll-indicator"

export function Demo() {
  return (
    <>
      <RollingBallScrollIndicator scrollContainerId="scroll-area" />
      <div id="scroll-area" className="h-[400px] overflow-y-auto">
        {/* Scrollable content */}
      </div>
    </>
  )
}`} />

## Props

<PropsTable
  data={[
    { prop: "scrollContainerId", type: "string", defaultValue: "'scroll-target'", description: "Container ID" },
    { prop: "direction", type: "'vertical' | 'horizontal'", defaultValue: "'vertical'", description: "Direction" }
  ]}
/>
