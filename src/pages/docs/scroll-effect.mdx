import { CodeBlock, Dependencies } from '@/components/docs/component-installation'
import { CLICommand } from '@/components/docs/cli-command'
import { PropsTable } from '@/components/docs/props-table'
import { ComponentPreview } from '@/components/docs/component-preview'

# Scroll Effect

A stacking card scroll effect where cards stack on top of each other as you scroll.

<ComponentPreview
  component={
    <div className="relative w-full rounded-lg overflow-hidden bg-black">
      <video autoPlay loop muted playsInline className="w-full h-auto max-h-[600px] object-cover">
        <source src="https://pub-affb2190b74549c4a9fde667b3cb7485.r2.dev/demos/stack-scroll.mp4" type="video/mp4" />
      </video>
    </div>
  }
  code={`import { ScrollEffect } from '@/components/ui/scroll-effect'

export function Demo() {
  return <ScrollEffect items={[{ image: "/1.jpg", title: "Card", description: "Desc" }]} />
}`}
/>

## Install using CLI

<CLICommand componentName="scroll-effect" />

## Install Manually

<Dependencies step={1} title="Install dependencies">
    <CodeBlock code="npm install framer-motion" />
</Dependencies>

<Dependencies step={2} title="Copy the source code">
    <p className="text-[13px] text-muted-foreground mt-2 mb-8">Copy into <code className="text-foreground font-mono text-xs bg-neutral-100 dark:bg-[#161616] px-2 py-1 rounded-md">components/ui/scroll-effect.tsx</code></p>
</Dependencies>

## Props

<PropsTable
  data={[
    { prop: "items", type: "{ image, title, description }[]", defaultValue: "-", description: "Card items" }
  ]}
/>
