import { CodeBlock, Dependencies } from '@/components/docs/component-installation'
import { CLICommand } from '@/components/docs/cli-command'
import { PropsTable } from '@/components/docs/props-table'
import { ComponentPreview } from '@/components/docs/component-preview'
import LightLines from '@/components/ui/light-lines'

# Light Lines

An animated background with vertical lines and traveling light particles. Creates a beautiful, dynamic background perfect for hero sections and landing pages.

<ComponentPreview
  component={
    <div className="relative w-full h-[400px] rounded-lg overflow-hidden">
      <LightLines />
    </div>
  }
  code={`import LightLines from '@/components/ui/light-lines'

export function Demo() {
  return (
    <div className="relative h-screen">
      <LightLines>
        <div className="flex items-center justify-center h-full">
          <h1 className="text-4xl font-bold text-white">Hero Content</h1>
        </div>
      </LightLines>
    </div>
  )
}`}
/>

## Install using CLI

<CLICommand componentName="light-lines" />

## Install Manually

<Dependencies step={1} title="Install dependencies">
    <CodeBlock code="npm install clsx tailwind-merge" />
</Dependencies>

<Dependencies step={2} title="Copy the source code">
    <p className="text-[13px] text-muted-foreground mt-2 mb-4">Copy into <code className="text-foreground font-mono text-xs bg-neutral-100 dark:bg-[#161616] px-2 py-1 rounded-md">components/ui/light-lines.tsx</code></p>
</Dependencies>

> **Note**: This is a large component (360+ lines) with SVG paths. Install via CLI for the full source code.

## Props

<PropsTable
  data={[
    { prop: "className", type: "string", defaultValue: "-", description: "Additional CSS classes" },
    { prop: "linesOpacity", type: "number", defaultValue: "0.05", description: "Opacity of static lines" },
    { prop: "lightsOpacity", type: "number", defaultValue: "0.9", description: "Opacity of animated lights" },
    { prop: "speedMultiplier", type: "number", defaultValue: "1", description: "Animation speed multiplier" },
    { prop: "gradientFrom", type: "string", defaultValue: "'#2462F6'", description: "Gradient start color" },
    { prop: "gradientTo", type: "string", defaultValue: "'#5999F8'", description: "Gradient end color" },
    { prop: "lightColor", type: "string", defaultValue: "'#fff'", description: "Color of animated lights" },
    { prop: "lineColor", type: "string", defaultValue: "'#fff'", description: "Color of static lines" },
    { prop: "children", type: "React.ReactNode", defaultValue: "-", description: "Content to overlay" },
  ]}
/>

## Customization

### Custom Colors

```tsx
<LightLines 
  gradientFrom="#8B5CF6"
  gradientTo="#EC4899"
  lightColor="#ffffff"
/>
```
