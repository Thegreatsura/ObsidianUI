import { CodeBlock, Dependencies } from '@/components/docs/component-installation'
import { CLICommand } from '@/components/docs/cli-command'
import { PropsTable } from '@/components/docs/props-table'
import { ComponentPreview } from '@/components/docs/component-preview'
import PixelatedImageTrail from '@/components/ui/pixelated-image-trail'

# Pixelated Image Trail

An interactive mouse trail effect that reveals pixelated images as you move your cursor. Creates a unique, engaging experience.

<ComponentPreview
  component={
    <div className="relative w-full h-[400px] rounded-lg overflow-hidden bg-zinc-950">
      <PixelatedImageTrail className="h-full" />
    </div>
  }
  code={`import PixelatedImageTrail from '@/components/ui/pixelated-image-trail'

export function Demo() {
  return <PixelatedImageTrail />
}`}
/>

## Install using CLI

<CLICommand componentName="pixelated-image-trail" />

## Install Manually

<Dependencies step={1} title="Install dependencies">
    <CodeBlock code="npm install framer-motion" />
</Dependencies>

<Dependencies step={2} title="Copy the source code">
    <p className="text-[13px] text-muted-foreground mt-2 mb-4">Copy into <code className="text-foreground font-mono text-xs bg-neutral-100 dark:bg-[#161616] px-2 py-1 rounded-md">components/ui/pixelated-image-trail.tsx</code></p>
</Dependencies>

> **Note**: Install via CLI for the full trail tracking implementation.

## Props

<PropsTable
  data={[
    { prop: "images", type: "string[]", defaultValue: "[...defaults]", description: "Array of image URLs for trail" },
    { prop: "className", type: "string", defaultValue: "-", description: "Additional CSS classes" },
    { prop: "slices", type: "number", defaultValue: "-", description: "Number of slices per image" },
    { prop: "smoothing", type: "number", defaultValue: "-", description: "Trail smoothing factor" },
  ]}
/>
