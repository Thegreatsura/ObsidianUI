import { CodeBlock, Dependencies } from '@/components/docs/component-installation'
import { CLICommand } from '@/components/docs/cli-command'
import { PropsTable } from '@/components/docs/props-table'
import { ComponentPreview } from '@/components/docs/component-preview'
import TestimonialsCard from '@/components/ui/testimonials-card'

export const demoItems = [
  { id: 1, title: "Sarah Chen", description: "This component library has transformed our development workflow. The animations are butter smooth and the code quality is exceptional!", image: "https://images.unsplash.com/photo-1494790108377-be9c29b29330?w=400&h=400&fit=crop" },
  { id: 2, title: "Michael Brown", description: "Incredible attention to detail. Every component feels premium and polished. Highly recommended for any serious project.", image: "https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=400&h=400&fit=crop" },
  { id: 3, title: "Emily Davis", description: "Best UI library I've used. The documentation is clear and components just work out of the box.", image: "https://images.unsplash.com/photo-1438761681033-6461ffad8d80?w=400&h=400&fit=crop" },
]

# Testimonials Card

An animated testimonial card stack with smooth 3D transitions between testimonials.

<ComponentPreview
  component={
    <div className="relative w-full h-[500px] rounded-lg overflow-hidden bg-white dark:bg-zinc-950 flex items-center justify-center">
      <TestimonialsCard items={demoItems} width={500} />
    </div>
  }
  code={`import TestimonialsCard from '@/components/ui/testimonials-card'

const testimonials = [
  { id: 1, title: "John Doe", description: "Great product!", image: "/avatar1.jpg" },
  { id: 2, title: "Jane Smith", description: "Love it!", image: "/avatar2.jpg" },
]

export function Demo() {
  return <TestimonialsCard items={testimonials} />
}`}
/>

## Install using CLI

<CLICommand componentName="testimonials-card" />

## Install Manually

<Dependencies step={1} title="Install dependencies">
    <CodeBlock code="npm install framer-motion lucide-react clsx tailwind-merge" />
</Dependencies>

<Dependencies step={2} title="Copy the source code">
    <p className="text-[13px] text-muted-foreground mt-2 mb-8">Copy into <code className="text-foreground font-mono text-xs bg-neutral-100 dark:bg-[#161616] px-2 py-1 rounded-md">components/ui/testimonials-card.tsx</code></p>
</Dependencies>

## Props

<PropsTable
  data={[
    { prop: "items", type: "TestimonialItem[]", defaultValue: "Required", description: "Array of testimonial items" },
    { prop: "width", type: "number", defaultValue: "400", description: "Card stack width" },
    { prop: "showNavigation", type: "boolean", defaultValue: "true", description: "Show navigation arrows" },
    { prop: "showCounter", type: "boolean", defaultValue: "true", description: "Show item counter" },
    { prop: "autoPlay", type: "boolean", defaultValue: "false", description: "Enable auto-play" }
  ]}
/>
